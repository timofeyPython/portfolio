export enum ERouters {
  users = "users",
  products = "products",
  types = "types",
  orders = "orders",
  messages = "messages",
  status = "status",
  works = "works",
  reviews = "reviews",
}

export enum ERoutesUser {
  reg = "registration",
  login = "login",
  check = "check",
}

export enum ERoutesType {
  categories = "categories",
  light = "light",
  colortemps = "colortemps",
  materials = "materials",
  colorframes = "colorframes",
}
